

function human(firstName, lastName, email, favoriteColor, birthday) {
    this.firstName = firstName,
    this.lastName = lastName,
    this.email = email,
    this.favoriteColor = favoriteColor,
    this.birthday = birthday
};
module.exports = { human };
